import 'package:flutter/material.dart';

class TransformadorFormScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Formulario de Mantenimiento a Transformadores'),
      ),
      body: Center(
        child: Text('Aquí irá el formulario de mantenimiento a transformadores'),
      ),
    );
  }
}
